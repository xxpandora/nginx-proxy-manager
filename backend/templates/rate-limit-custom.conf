{% if rate_limit_basic == 1 or rate_limit_basic == true %}
  # Rate Limit Basic
limit_conn perip_limit_{{ id }} {{ rate_conn }};
limit_conn_status 444;

limit_req zone=dos_flood_limit_{{ id }} burst={{ rate_burst }} nodelay;
limit_req_status 444;
{% endif %}
