{% if rate-limit_basic == 1 or rate-limit_basic == true %}
  # Rate Limit Basic
limit_conn perip_limit_{{ id }} 10;
limit_conn_status 444;

limit_req zone=dos_flood_limit_{{ id }} burst=10 nodelay;
limit_req_status 444;
{% endif %}
